import { Button } from "@mui/material";
import { styled } from "@mui/material/styles"; // Изменен импорт

export const AddToCardButton = styled(Button)(({theme}) => `
 && 
    {
    background-color: ${theme.palette.primary.main};

    }
`);



