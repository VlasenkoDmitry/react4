import { styled } from "styled-components";
import { Box } from "@mui/material";

export const BoxMain = styled(Box)`
  && {
    width: 100%;
  }
`;

export const BoxParts = styled(Box)(
  ({ theme }) => `
&& {

  }
`
);
