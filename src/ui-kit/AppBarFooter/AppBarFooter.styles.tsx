import { styled } from "styled-components";
import { AppBar } from "@mui/material";

export const AppBarFooter = styled(AppBar)`
  top: auto;
  bottom: 0;
  background-сolor: red;
  box-sizing: border-box;
`;
